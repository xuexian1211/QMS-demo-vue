- [ ] 1. 重构 `CustomerSupplier.vue` 的前端布局结构，移除原有“客商合一 CRUD”的表单和逻辑，改为映射关系列表（包含客户、供应商、映射类型、状态等列）。
- [ ] 2. 在 `CustomerSupplier.vue` 的“新增映射”弹窗中，实现引用 `CustomerArchive` 和 `SupplierArchive` 数据的下拉选择器（使用 Mock 数据模拟现有客户和供应商列表供选择）。
- [ ] 3. 实现新增、编辑和状态切换（启用/禁用）的前端交互逻辑。
- [ ] 4. 优化映射列表的搜索栏，支持按“客户名称”和“供应商名称”双向查询过滤。
- [ ] 5. 将 `MainLayout.vue` 中对客供关系的菜单项链接从当前的"建设中(Developing)"状态解除，指向开发好的 `CustomerSupplier` 路由视图。
- [ ] 6. 进行 UI 组件和交互的手动测试验证，确保数据的引用和映射关系的展示正确无误。
