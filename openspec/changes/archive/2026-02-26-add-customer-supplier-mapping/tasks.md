- [x] 1. 废弃原有的 `CustomerSupplier.vue`（或将其改造），重新规划并建立左树右表或 Tabs 页签设计的结构，分出"系统集成编码映射"、"CTQ与标准协同映射"、"产品追溯关系配置"等子业务模块页签。
- [x] 2. 在前端实现"客供物料编码映射"的 CRUD 界面，包含客户/供应商选择、外部物料编码以及内部标准物料(MaterialProduct)的交互绑定功能。
- [x] 3. 在前端实现"CTQ映射协同设置"视图，允许将客户的特定质量要求(CTQ)挂载或同步至 QMS 的检验项目（InspectionItem）列表中。
- [x] 4. 修改 `MainLayout.vue`，将菜单项更新并连接至新的全链路客供关系映射组件页面。
- [x] 5. 构建 Mock 数据链以验证"客户物料编码 -> 内部物料 -> 供应商追溯"的基础映射功能，验证界面的逻辑连贯性。
- [x] 6. 运行 `npm run dev` 或 `npx vite dev` 测试前端应用，执行人工交互验收。
